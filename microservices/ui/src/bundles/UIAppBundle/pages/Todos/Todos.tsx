import * as React from 'react';
import { Layout } from "../../components";

export const TodosPage = () => {
    return (
        <Layout>
            <h1>This is the todos page list!</h1>
        </Layout>
    );
}